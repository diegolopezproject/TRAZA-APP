/** Product icon implementations remain in src/components/icons until the SVG audit migration. */
export const iconContract = { size: "1em", stroke: "currentColor", decorative: "aria-hidden", interactive: "label-on-control" } as const;
