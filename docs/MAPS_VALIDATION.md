# Maps validation

Map links use `https://www.google.com/maps/search/?api=1&query=` with a precise London place query. The UI always exposes “Abrir en Google Maps”; it does not embed maps or scrape Google content. The query is owned by the repository seed/media boundary and can be edited for user-created places.
